services:
  mya:
    build: .
    container_name: mya
    restart: unless-stopped
    command: uvicorn server:app --host 0.0.0.0 --port 8080
    ports:
      - "8080:8080"
    volumes:
      - mya_data:/app/data

volumes:
  mya_data:
